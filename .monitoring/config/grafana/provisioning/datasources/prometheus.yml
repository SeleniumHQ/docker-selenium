apiVersion: 1

datasources:
  - name: prometheus
    type: prometheus
    uid: selenium-prometheus
    url: http://prometheus:9090
    access: proxy
    isDefault: true
    jsonData:
      timeInterval: 15s
